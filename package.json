{
  "name": "web-pack-example",
  "version": "2.0.0",
  "description": "wep-pack sample project ",
  "author": "dharmendra.patel26@gmail.com",
  "license": "ISC",
  "main": "index.js",
  "keywords": [
    "webpack"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "publish-bugFix": "npm version patch -m 'upgraded to %s for test' && git push && git push --tags",
    "publish-minor": "npm version minor -m 'upgraded to %s for test' && git push && git push --tags",
    "publish-release": "npm version major -m 'upgraded to %s for test' && git push && git push --tags"
  },
  "devDependencies": {
    "webpack": "^4.5.0"
  },
  "engines": {
    "node": ">= 7.10.0",
    "npm": ">= 4.2.0"
  }
}
